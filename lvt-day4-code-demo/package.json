{
  "name": "taskflow-demo",
  "version": "1.0.0",
  "description": "TaskFlow — a small Express API used as the target codebase for the Day 4 Claude Code orchestration demos (subagents, agent teams, dynamic workflows).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^2.1.8"
  }
}
